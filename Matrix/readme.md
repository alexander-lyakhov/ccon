# The Matrix rain effect for `windows` console

> Inspired by `cmatrix` for Linux

|Keyboard shortcuts| Action|
|------------------|--------|
| `1..9`           | Speed control
| `Esc` or `q`     | Exit

<img src="./matrix.gif" />
